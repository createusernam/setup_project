{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "pipeline-machine.schema.json",
  "title": "Setup pipeline machine contract",
  "type": "object",
  "required": ["version", "risk_policy", "transitions", "invalidations", "verification_layers"],
  "properties": {
    "version": {"type": "string"},
    "risk_policy": {
      "type": "object",
      "required": ["default", "tiers", "selection_factors", "skip_contract"],
      "properties": {
        "default": {"enum": ["T0", "T1", "T2", "T3", "T4"]},
        "tiers": {
          "type": "object",
          "minProperties": 5,
          "additionalProperties": {
            "type": "object",
            "required": ["label", "criteria", "required_phases"],
            "properties": {
              "label": {"type": "string", "minLength": 1},
              "criteria": {"type": "string", "minLength": 1},
              "required_phases": {"type": "array", "items": {"type": "string"}},
              "conditional_phases": {"type": "object", "additionalProperties": {"type": "string", "minLength": 1}}
            }
          }
        }
      }
    },
    "transitions": {
      "type": "object",
      "additionalProperties": {
        "type": "object",
        "required": ["skill", "tiers", "requires"],
        "properties": {
          "skill": {"type": "string", "minLength": 1},
          "tiers": {"type": "array", "minItems": 1, "items": {"enum": ["T0", "T1", "T2", "T3", "T4"]}},
          "requires": {
            "type": "array",
            "items": {
              "type": "object",
              "required": ["artifact"],
              "properties": {
                "artifact": {"type": "string", "minLength": 1},
                "attested": {"type": "boolean"},
                "json_pointer": {"type": "string"},
                "equals": {},
                "in": {"type": "array"},
                "tiers": {"type": "array", "items": {"enum": ["T0", "T1", "T2", "T3", "T4"]}}
              }
            }
          },
          "human_gate": {"type": "string"}
        }
      }
    },
    "invalidations": {"type": "object"},
    "verification_layers": {"type": "array", "minItems": 5}
  }
}
